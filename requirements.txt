# ADP — Python dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database (ADR-001)
sqlalchemy>=2.0.36
alembic>=1.13.0
asyncpg>=0.30.0          # async driver for runtime
psycopg2-binary>=2.9.9   # sync driver for alembic migrations

# LLM routing (ADR-002)
litellm>=1.50.0

# Configuration
pydantic>=2.8.0
pydantic-settings>=2.4.0

# HTTP client (used by integrations: Jira, GitHub, Slack)
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
pytest-mock>=3.14.0
